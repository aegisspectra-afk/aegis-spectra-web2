[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Note: @netlify/plugin-emails is not included (we use Gmail SMTP instead)

[build.environment]
  NODE_VERSION = "18"

